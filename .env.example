# .env.example
PORT=4000
NODE_ENV=development
LOG_LEVEL=info

# Optional: Redis URL (set for production / multi-instance)
# Example: REDIS_URL=redis://localhost:6379
REDIS_URL=

# JWT secret used by server auth helpers
JWT_SECRET=changeme
